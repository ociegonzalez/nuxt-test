generator client {
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
}

model SiteReview {
  id    Int     @id @default(autoincrement())
  name String
  subtitle String
  profileImage String
  description String
  createdAt DateTime @default(now())
}